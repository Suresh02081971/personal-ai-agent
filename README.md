# Personal AI Agent (SureshAI)

A LangGraph-powered personal AI agent for development and automation.

## Setup
```bash
git clone <repo>
cd personal-ai-agent
pip install -r requirements.txt

Sync after venv ignore
